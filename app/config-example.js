const config = {
  telegram: {
    token: "", // telegram-bot token
    chat_id: "", // chat_id from telegram
  },
  server: {
    port: 3000,
    token: "", // Math.random().toString(36).substr(2) + Math.random().toString(36).substr(2);
  }
  };
export default config;
